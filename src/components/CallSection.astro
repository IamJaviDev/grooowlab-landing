<section class="call" id="contacto">
  <div class="call-container">
    <!-- LEFT -->

    <div class="call-left">
      <img src="/growlab-logo.png" class="call-logo" alt="logo" />

      <h2>¿Listo para escalar tu marca de forma rentable?</h2>

      <p class="call-sub">
        En 20 minutos te daremos claridad sobre dónde estás, qué frena tu
        crecimiento y si somos el partner adecuado para desbloquear tu siguiente
        etapa.
      </p>

      <div class="call-box">
        <h3>Ideal para ti si quieres:</h3>

        <ul>
          <li>Un partner estratégico, no otra agencia más</li>
          <li>Un sistema de captación y retención que funcione de verdad</li>
          <li>Resultados reales sin perder rentabilidad</li>
        </ul>
      </div>

      <div class="call-box">
        <h3>Para aplicar, tu marca debe:</h3>

        <ul>
          <li>Facturar al menos 30K€/mes</li>
          <li>Estar lista para escalar</li>
        </ul>
      </div>
    </div>

    <!-- RIGHT -->

    <div class="call-right">
      <iframe
        src="https://calendly.com/"
        width="100%"
        height="100%"
        frameborder="0"
      >
      </iframe>
    </div>
  </div>
</section>

<style>
  .call {
    padding: clamp(70px, 8vw, 120px) 20px;
    max-width: 1400px;
    margin: auto;
  }

  /* GRID */

  .call-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(40px, 6vw, 70px);
    align-items: center;
  }

  /* LOGO */

  .call-logo {
    width: 20%;
    margin-bottom: 25px;
    filter: drop-shadow(0 0 10px var(--color-accent))
      drop-shadow(0 0 25px var(--color-accent));
  }

  /* TITLE */

  .call-left h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: 15px;
  }

  /* SUBTEXT */

  .call-sub {
    color: #aaa;
    margin-bottom: 30px;
    max-width: 500px;
    font-size: clamp(0.95rem, 1vw, 1.05rem);
  }

  /* BOX */

  .call-box {
    background: linear-gradient(
      90deg,
      rgba(255, 255, 255, 0.06),
      rgba(255, 255, 255, 0.02)
    );

    padding: clamp(20px, 2vw, 25px);
    border-radius: 16px;
    margin-bottom: 20px;
  }

  /* BOX TITLE */

  .call-box h3 {
    margin-bottom: 10px;
    font-size: clamp(0.95rem, 1vw, 1rem);
  }

  /* LIST */

  .call-box ul {
    list-style: none;
    padding: 0;
  }

  .call-box li {
    margin: 8px 0;
    color: #aaa;
    position: relative;
    padding-left: 18px;
    font-size: 0.95rem;
  }

  /* BULLET */

  .call-box li::before {
    content: "";
    width: 6px;
    height: 6px;
    background: var(--color-accent);
    border-radius: 50%;
    position: absolute;
    left: 0;
    top: 9px;
  }

  /* CALENDLY */

  .call-right {
    height: 520px;
    border-radius: 12px;
    overflow: hidden;
    background: #fff;
  }

  .call-right iframe {
    width: 100%;
    height: 100%;
    border: 0;
  }

  /* TABLET */

  @media (max-width: 900px) {
    .call-container {
      grid-template-columns: 1fr;
      gap: 50px;
    }

    .call-left {
      text-align: left;
      max-width: 600px;
      margin: auto;
    }

    .call-left h2 {
      text-align: center;
    }

    .call-sub {
      text-align: center;
      margin: auto auto 30px;
    }
  }

  /* MOBILE */

  @media (max-width: 600px) {
    .call-right {
      height: 620px;
    }
  }
</style>
