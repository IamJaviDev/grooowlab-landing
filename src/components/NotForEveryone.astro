<section class="not">
  <div class="not-top">
    <img src="/growlab-logo.png" class="not-logo" alt="logo" />

    <h2>GrooowLab no es para todo el mundo.</h2>

    <p>
      Nuestro compromiso lo es todo, priorizamos trabajar con pocas marcas, para
      implicarnos al 100%.
    </p>
  </div>

  <div class="not-grid">
    <div class="not-card">
      <h3>Fundadores que valoran la estrategia</h3>

      <p>
        No nos limitamos a “subir campañas”. Buscas un partner que piense en
        sistemas, márgenes y procesos contigo.
      </p>
    </div>

    <div class="not-card">
      <h3>Marcas que quieren escalar a +7 cifras</h3>

      <p>
        Trabajamos con marcas que ya han validado su producto y quieren pasar al
        siguiente nivel.
      </p>
    </div>

    <div class="not-card">
      <h3>Marcas cansadas de agencias</h3>

      <p>
        Si has probado agencias que solo meten tráfico, aquí vas a encontrar
        análisis, foco y estructura.
      </p>
    </div>
  </div>
</section>

<style>
  .not {
    padding: clamp(70px, 8vw, 120px) 20px;
    text-align: center;
    max-width: 1200px;
    margin: auto;
  }

  /* TOP */

  .not-top {
    margin-bottom: clamp(50px, 6vw, 80px);
  }

  /* LOGO */

  .not-logo {
    width: 10%;
    margin: 0 auto 25px;
    filter: drop-shadow(0 0 15px var(--color-accent));
  }

  /* TITLE */

  .not h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 400;
    margin-bottom: 15px;
  }

  /* TEXT */

  .not-top p {
    color: #aaa;
    max-width: 600px;
    margin: auto;
    font-size: clamp(0.95rem, 1vw, 1.05rem);
  }

  /* GRID */

  .not-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(18px, 3vw, 30px);
    margin-top: 60px;
  }

  /* CARD */

  .not-card {
    max-width: 320px;
    padding: clamp(30px, 3vw, 40px) clamp(22px, 2vw, 30px);
    border-radius: 20px;
    border: 1px solid rgba(120, 120, 120, 0.08);

    background: linear-gradient(
      180deg,
      rgba(224, 255, 63, 0.06) 0%,
      rgba(224, 255, 63, 0.03) 40%,
      rgba(0, 0, 0, 0) 100%
    );

    transition: 0.25s;
  }

  /* CARD TEXT */

  .not-card h3 {
    font-size: clamp(1.1rem, 1.3vw, 1.3rem);
    margin-bottom: 15px;
  }

  .not-card p {
    color: #aaa;
    font-size: 0.95rem;
    line-height: 1.6;
  }

  /* TABLET */

  @media (max-width: 900px) {
    .not-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* MOBILE */

  @media (max-width: 600px) {
    .not-grid {
      grid-template-columns: 1fr;
      max-width: 420px;
      margin: 60px auto 0;
    }

    .not h2 {
      font-size: clamp(1.8rem, 6vw, 2.3rem);
    }
  }
</style>
