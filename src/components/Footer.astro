<footer class="footer">
  <div class="footer-line"></div>

  <div class="footer-container">
    <!-- LEFT -->

    <div class="footer-left">
      <img src="/growlab-logo.png" class="footer-logo" alt="logo" />

      <div class="footer-social">
        <a href="https://instagram.com" target="_blank" aria-label="Instagram">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="22"
            height="22"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.8"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
            <circle cx="12" cy="12" r="4"></circle>
            <circle cx="17.5" cy="6.5" r="1.2"></circle>
          </svg>
        </a>
      </div>
    </div>

    <!-- CENTER -->

    <nav class="footer-nav">
      <a href="#servicios">Servicios</a>
      <a href="#resultados">Nuestros resultados</a>
      <a href="#sistema">Nuestro Proceso</a>
      <a href="#contacto">Contacta</a>
    </nav>

    <!-- RIGHT -->

    <div class="footer-right">
      <a href="#contact" class="footer-cta">
        Agenda tu Discovery Call Gratuita
      </a>

      <div class="footer-legal">
        <a href="#">Aviso legal</a>
        <a href="#">Política de privacidad</a>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding: clamp(60px, 7vw, 100px) 20px;
    max-width: 1400px;
    margin: auto;
  }

  /* línea superior */

  .footer-line {
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.15),
      transparent
    );
    margin-bottom: clamp(30px, 5vw, 50px);
  }

  /* GRID */

  .footer-container {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 40px;
  }

  /* LEFT */

  .footer-left {
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  .footer-logo {
    width: 120px;
    filter: drop-shadow(0 0 10px var(--color-accent))
      drop-shadow(0 0 20px var(--color-accent));
  }

  /* SOCIAL */

  .footer-social {
    display: flex;
    gap: 15px;
  }

  .footer-social a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: 8px;
    color: #aaa;
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: 0.2s;
  }

  .footer-social a:hover {
    background: rgba(255, 255, 255, 0.05);
    transform: translateY(-2px);
  }

  /* NAV */

  .footer-nav {
    display: flex;
    gap: clamp(18px, 3vw, 30px);
    justify-content: center;
  }

  .footer-nav a {
    color: #aaa;
    text-decoration: none;
    font-size: 0.9rem;
    transition: 0.2s;
  }

  .footer-nav a:hover {
    color: #fff;
  }

  /* RIGHT */

  .footer-right {
    text-align: right;
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  /* CTA */

  .footer-cta {
    display: inline-block;
    padding: 12px 26px;
    border-radius: 8px;
    background: var(--color-accent);
    color: #000;
    font-weight: 600;
    box-shadow: 0 0 25px rgba(224, 255, 63, 0.35);
    font-size: 0.9rem;
  }

  /* LEGAL */

  .footer-legal {
    font-size: 0.8rem;
    color: #777;
    display: flex;
    justify-content: flex-end;
    gap: 15px;
  }

  .footer-legal a {
    color: #777;
    text-decoration: none;
  }

  /* TABLET */

  @media (max-width: 900px) {
    .footer-container {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 40px;
    }

    .footer-left {
      align-items: center;
    }

    .footer-nav {
      flex-wrap: wrap;
    }

    .footer-right {
      text-align: center;
      align-items: center;
    }

    .footer-legal {
      justify-content: center;
    }
  }

  /* MOBILE */

  @media (max-width: 600px) {
    .footer-nav {
      flex-direction: column;
      gap: 12px;
    }
  }
</style>
