<section class="videos">
  <div class="container">
    <p class="videos__tag">+10M generados para nuestros clientes</p>

    <h2 class="videos__title">Ellos ya están por encima de las 7 cifras</h2>

    <div class="videos__grid">
      <!-- CARD -->

      <div class="video-card">
        <div class="video-card__media">
          <!-- aquí irá el video -->
        </div>

        <div class="video-card__play">▶</div>
      </div>

      <div class="video-card">
        <div class="video-card__media"></div>

        <div class="video-card__play">▶</div>
      </div>

      <div class="video-card">
        <div class="video-card__media"></div>

        <div class="video-card__play">▶</div>
      </div>
    </div>

    <div class="videos__cta">
      <a class="btn"> Descubre cómo podemos ayudarte </a>
    </div>
  </div>
</section>

<style>
  .videos {
    padding: 140px 0;
    text-align: center;
  }

  .videos__tag {
    color: var(--color-accent);
    margin-bottom: 10px;
  }

  .videos__title {
    font-size: clamp(2.5rem, 4vw, 3rem);
    margin-bottom: 60px;
  }

  .videos__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
    max-width: 900px;
    margin: auto;
  }

  .video-card {
    position: relative;
    border-radius: 18px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .video-card__media {
    aspect-ratio: 9/16;
    background: #1a1a1a; /* aquí irán los vídeos */
  }

  .video-card__play {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: rgba(0, 0, 0, 0.6);
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.4rem;
  }

  .videos__cta {
    margin-top: 50px;
  }

  .btn {
    background: var(--color-accent);
    color: #000;
    padding: 14px 24px;
    border-radius: 999px;
    font-weight: 600;
  }
</style>
