<section class="hero">
  <div class="container hero__grid">
    <div class="hero__content">
      <h1>
        El 90% de <br /><span class="accent serif">
          eCommerce no llega a<br /> 7 cifras.</span
        ><br />
        <span class="sub_title"
          >El tuyo tampoco. Pero oye, la web quedó preciosa.</span
        >
      </h1>

      <p>
        No es tu producto. No es el mercado. Es que estás intentando escalar sin
        sistema — y eso es como intentar ganar Le Mans con un Seat Ibiza.
        Nosotros construimos el motor. Tú aceleras.
      </p>

      <a href="#servicios" class="hero__cta"
        >Descubre si tu marca tiene madera de 7 cifras</a
      >
    </div>

    <div class="hero__visual">
      <div class="hero__column hero__column--up">
        <img src="/metrica1.JPG" />
        <img src="/banner6.jpg" />
        <img src="/metrica3.jpg" />
        <img src="/banner2.jpg" />
        <img src="/metrica1.JPG" />
        <img src="/banner6.jpg" />
        <img src="/metrica3.jpg" />
        <img src="/banner2.jpg" />
        <img src="/metrica5.jpg" />
      </div>

      <div class="hero__column hero__column--down">
        <img src="/banner8.jpg" />
        <img src="/banner1.jpg" />
        <img src="/metrica2.JPG" />
        <img src="/metrica3.jpg" />
        <img src="/banner8.jpg" />
        <img src="/banner1.jpg" />
        <img src="/metrica2.JPG" />
        <img src="/banner2.jpg" />
        <img src="/metrica3.jpg" />
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: clamp(60px, 8vw, 120px) 0;
    overflow: hidden;
  }

  .hero__grid {
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    align-items: center;
    gap: clamp(30px, 5vw, 60px);
  }

  /* -------------------------
   TEXT
------------------------- */

  .hero__content h1 {
    font-size: clamp(2.4rem, 5.8vw, 4.6rem);
    line-height: 1.05;
    margin-bottom: 20px;
  }

  .hero__content h1 .sub_title {
    display: block;
    font-size: clamp(2rem, 5vw, 4rem) !important;
    line-height: 1.05;
  }

  .hero__content p {
    max-width: 500px;
    opacity: 0.8;
    margin-bottom: 24px;
    font-size: clamp(1.1rem, 1.4vw, 1.3rem);
  }

  .hero__cta {
    display: inline-block;
    background: var(--color-accent);
    color: #000;
    padding: 14px 22px;
    border-radius: 8px;
    cursor: pointer;
    font-size: clamp(0.95rem, 1.2vw, 1.1rem);
    font-weight: 600;
    box-shadow: 0 0 25px #e0ff3f55;
  }

  /* -------------------------
   VISUAL
------------------------- */

  .hero__visual {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    height: clamp(350px, 60vh, 700px);
    overflow: hidden;
    position: relative;
  }

  .hero__visual::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 120px;
    background: linear-gradient(to bottom, var(--color-bg), transparent);
    z-index: 2;
    pointer-events: none;
  }

  .hero__visual::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 120px;
    background: linear-gradient(to top, var(--color-bg), transparent);
    z-index: 2;
    pointer-events: none;
  }

  .hero__column {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .hero__column img {
    border-radius: 12px;
    width: 100%;
    object-fit: cover;
  }

  .hero__column--up {
    animation: scrollUp 20s linear infinite;
  }

  .hero__column--down {
    animation: scrollDown 20s linear infinite;
  }

  @keyframes scrollUp {
    0% {
      transform: translateY(0);
    }
    100% {
      transform: translateY(-50%);
    }
  }

  @keyframes scrollDown {
    0% {
      transform: translateY(-50%);
    }
    100% {
      transform: translateY(0);
    }
  }

  /* -------------------------
   TABLET
------------------------- */

  @media (max-width: 1024px) {
    .hero__grid {
      grid-template-columns: 1fr;
    }

    .hero__content {
      text-align: center;
      max-width: 650px;
      margin: auto;
    }

    .hero__content p {
      margin-left: auto;
      margin-right: auto;
    }

    .hero__visual {
      margin-top: 40px;
    }
  }

  /* -------------------------
   MOBILE
------------------------- */
  @media (max-width: 900px) {
    .hero__grid {
      grid-template-columns: 1fr;
    }

    .hero__visual {
      display: none;
    }
  }
  @media (max-width: 640px) {
    .hero__visual {
      height: 320px;
      gap: 14px;
    }

    .hero__column {
      gap: 14px;
    }

    .hero__cta {
      width: 100%;
      text-align: center;
    }
  }
</style>
