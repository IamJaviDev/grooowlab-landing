<section class="services">
  <div class="container">
    <p class="services__tag">Servicios</p>

    <h2 class="services__title">El sistema completo para escalar</h2>

    <div class="accordion">
      <details class="accordion__item">
        <summary>
          Gestión de Paid Media
          <span>+</span>
        </summary>

        <p>
          Diseñamos y optimizamos campañas publicitarias en Meta, Google y
          TikTok enfocadas en adquisición rentable. Analizamos continuamente el
          rendimiento para escalar la inversión manteniendo un ROAS positivo.
        </p>
      </details>

      <details class="accordion__item">
        <summary>
          Email Marketing
          <span>+</span>
        </summary>

        <p>
          Creamos flujos automatizados y campañas estratégicas que aumentan la
          recurrencia de compra. Segmentamos audiencias y optimizamos cada envío
          para maximizar ingresos por cliente.
        </p>
      </details>

      <details class="accordion__item">
        <summary>
          Estrategia creativa
          <span>+</span>
        </summary>

        <p>
          Desarrollamos creatividades basadas en datos y psicología de compra.
          Testeamos constantemente anuncios, hooks y formatos para encontrar los
          mensajes que generan mayor conversión.
        </p>
      </details>

      <details class="accordion__item">
        <summary>
          Conversion Rate Optimization (CRO)
          <span>+</span>
        </summary>

        <p>
          Analizamos el comportamiento de los usuarios en tu tienda para mejorar
          cada paso del funnel. Implementamos mejoras en landing pages, checkout
          y experiencia de usuario para aumentar la tasa de conversión.
        </p>
      </details>

      <details class="accordion__item">
        <summary>
          Financial Analysis
          <span>+</span>
        </summary>

        <p>
          Estudiamos la rentabilidad real de cada canal y campaña. Calculamos
          métricas clave como MER, CAC y LTV para tomar decisiones de inversión
          basadas en datos financieros sólidos.
        </p>
      </details>
    </div>

    <div class="services__cta">
      <a class="btn"> Descubre cómo podemos ayudarte </a>
    </div>
  </div>
</section>

<style>
  .services {
    padding: 140px 0;
    text-align: center;
  }

  .services__tag {
    color: var(--color-accent);
    margin-bottom: 10px;
  }

  .services__title {
    font-size: clamp(2.5rem, 4vw, 3.5rem);
    margin-bottom: 60px;
  }

  .accordion {
    max-width: 900px;
    margin: auto;
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .accordion__item {
    background: linear-gradient(90deg, #1a1a1a, #2a2a2a);
    border-radius: 14px;
    padding: 22px 26px;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  summary {
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    cursor: pointer;
    font-size: 1.1rem;
    color: var(--color-accent);
  }

  summary span {
    font-size: 1.5rem;
  }

  .accordion__item p {
    margin-top: 16px;
    opacity: 0.8;
    line-height: 1.6;
    text-align: left;
  }

  .services__cta {
    margin-top: 50px;
  }

  .btn {
    background: var(--color-accent);
    color: #000;
    padding: 14px 24px;
    border-radius: 999px;
    font-weight: 600;
  }
</style>
